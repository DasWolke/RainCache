{
  "name": "raincache",
  "version": "0.5.0",
  "description": "A pluggable discord cache worker",
  "main": "index.js",
  "engines": {
    "node": ">=8.0.0"
  },
  "scripts": {
    "build:docs": "node_modules/.bin/docma -c docma.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "wolke <wolke@weeb.sh>",
  "license": "MIT",
  "optionalDependencies": {
    "amqplib": "^0.5.2",
    "redis": "^2.8.0"
  },
  "dependencies": {
    "tsubaki": "^1.3.0"
  },
  "devDependencies": {
    "eslint": "^4.19.1",
    "docma": "^2.1.0"
  }
}
